{
  "id": "quiz",
  "title": "Node.js and Stdin Check",
  "description": "Running scripts, stdin, and string input basics.",
  "lessonId": "01.1.1-running-javascript-node-js",
  "graphIndex": "01.1.1",
  "questions": [
    {
      "id": "q1",
      "prompt": "How do you run `starter/index.js` with Node.js?",
      "options": [
        { "id": "a", "text": "`node starter/index.js`" },
        { "id": "b", "text": "`npm start starter/index.js`" },
        { "id": "c", "text": "`javascript starter/index.js`" },
        { "id": "d", "text": "`open starter/index.js`" }
      ],
      "correctOptionId": "a",
      "explanation": "Node executes `.js` files with the `node` command. This is the standard way to run HackerRank-style solutions locally."
    },
    {
      "id": "q2",
      "prompt": "Input from HackerRank `stdin` arrives as which type?",
      "options": [
        { "id": "a", "text": "String" },
        { "id": "b", "text": "Number" },
        { "id": "c", "text": "Boolean" },
        { "id": "d", "text": "Depends on the first character" }
      ],
      "correctOptionId": "a",
      "explanation": "Stdin lines are always strings until you parse them explicitly with `Number()` or similar."
    },
    {
      "id": "q3",
      "prompt": "What does `\"  Ana  \".trim()` return?",
      "options": [
        { "id": "a", "text": "`\"Ana\"`" },
        { "id": "b", "text": "`\"  Ana  \"`" },
        { "id": "c", "text": "`\"Ana  \"`" },
        { "id": "d", "text": "`undefined`" }
      ],
      "correctOptionId": "a",
      "explanation": "`trim()` removes leading and trailing whitespace only, leaving the inner text intact."
    },
    {
      "id": "q4",
      "prompt": "Given `const line = \"42\"`, what is `typeof line`?",
      "options": [
        { "id": "a", "text": "`\"string\"`" },
        { "id": "b", "text": "`\"number\"`" },
        { "id": "c", "text": "`\"object\"`" },
        { "id": "d", "text": "`\"integer\"`" }
      ],
      "correctOptionId": "a",
      "explanation": "Even numeric-looking input stays a string until converted. `typeof \"42\"` is `\"string\"`."
    },
    {
      "id": "q5",
      "prompt": "Why call `rl.close()` after handling input in a one-shot CLI?",
      "options": [
        { "id": "a", "text": "Ends the readline interface and lets the process exit cleanly" },
        { "id": "b", "text": "Flushes `console.log` buffers to disk" },
        { "id": "c", "text": "Converts stdin strings to numbers automatically" },
        { "id": "d", "text": "Required before using template literals" }
      ],
      "correctOptionId": "a",
      "explanation": "Without closing the interface, the Node process may keep waiting for more input instead of finishing."
    }
  ]
}

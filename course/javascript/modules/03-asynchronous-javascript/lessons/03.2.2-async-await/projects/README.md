# Projects — Async/Await

Practice exercises for this lesson. Run each project with:

```bash
node starter/index.js
```

## What you should practice

- Sequential `await` steps
- `try/catch` around awaited rejections
- Async function execution order

## Project catalog

### 003-concurrency-limiter/

Run three async steps in order with `await`; catch a failing step (sequential async runner).
